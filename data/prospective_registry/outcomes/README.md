No checked-in `prospective_forecast_outcome_log` artifacts ship with this PR.

Realized outcomes should be added here only when the repository has actual
follow-up evidence to log against an existing frozen registration.
