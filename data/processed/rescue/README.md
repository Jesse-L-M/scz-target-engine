# Rescue Frozen Outputs

This subtree contains governed rescue datasets that are referenced directly by
checked-in rescue dataset cards.

The files here are frozen outputs, not mutable work products. Any update must land
with an updated task card, dataset cards, freeze manifest, split manifest, and
raw-to-frozen lineage artifact.
