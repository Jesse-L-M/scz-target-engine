{
  "artifact_name": "decision_vectors_v1",
  "schema_version": "v1",
  "file_format": "json",
  "description": "Additive v1 decision-vector payload.",
  "key_fields": [
    "schema_version"
  ],
  "required_fields": [
    {
      "name": "schema_version",
      "field_type": "string",
      "required": true,
      "description": "Decision-vector payload schema version."
    },
    {
      "name": "decision_head_definitions",
      "field_type": "object[]",
      "required": true,
      "description": "Current decision head definitions."
    },
    {
      "name": "domain_head_definitions",
      "field_type": "object[]",
      "required": true,
      "description": "Current domain profile definitions."
    },
    {
      "name": "entities",
      "field_type": "object",
      "required": true,
      "description": "Per-entity gene and module vectors."
    }
  ],
  "optional_fields": []
}
